<li>
    <button class = "change-devoured mb-1 burger-button" data-id = "{{id}}" data-devoured = "{{devoured}}">
        {{#if devoured}}order it{{else}}eat it{{/if}}
    </button>
    {{burger_name}}
</li>